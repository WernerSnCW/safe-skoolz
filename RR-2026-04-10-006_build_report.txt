========================================================================
BUILD REPORT — RR-2026-04-10-006
Track: B
Date: 2026-04-12
========================================================================

PRE-READ FINDINGS
------------------------------------------------------------------------

1. artifacts/safeschool/src/pages/dashboard/PupilDashboard.tsx
   - Line 225: Confirmed type: "message" in UrgentHelpDialog POST body
   - This was the root cause of urgent help messages not appearing in
     the parent child-alerts endpoint (which filters on type "urgent_help")

2. artifacts/api-server/src/routes/schools.ts
   - POST /pupils/reset-pin/:pupilId (line 210)
   - Line 238 confirmed: already sets { pinHash, failedLoginAttempts: 0,
     lockedUntil: null }
   - DECISION: Using existing reset-pin endpoint. No new unlock endpoint
     needed. The reset-pin endpoint clears the lockout AND generates a
     new PIN in a single operation.

3. artifacts/safeschool/src/pages/dashboard/CoordinatorDashboard.tsx
   - Overview tab confirmed at line 165: contains stat cards grid
     (Incidents, Protocols, Report) followed by Training Completion card
   - Locked pupils card inserted between stat cards grid and training card

========================================================================

CHANGES IMPLEMENTED
------------------------------------------------------------------------

CHANGE 1 — FIX-00 (CRITICAL)
  File: artifacts/safeschool/src/pages/dashboard/PupilDashboard.tsx
  Line changed: 225 (was type: "message", now type: "urgent_help")
  Verification: GET /messages/child-alerts in messages.ts filters on
  type === "urgent_help". This fix ensures urgent help messages sent by
  pupils are now visible to parents via that endpoint. The notification
  trigger in messages.ts also correctly fires "urgent_help_request"
  instead of falling through to "pupil_message".

CHANGE 2 — FIX-01a: Diary audit logs
  File: artifacts/api-server/src/routes/diary.ts
  - POST /diary/entries: writeAudit() added after db.insert().returning()
    and before res.status(201).json(). Event: "diary_entry_created".
  - DELETE /diary/entries/:id: writeAudit() added after db.delete() and
    before res.json(). Event: "diary_entry_deleted".
  Both calls happen AFTER successful database operations as required.

CHANGE 3 — FIX-01b: Notification acknowledge audit log
  File: artifacts/api-server/src/routes/notifications.ts
  - PATCH /notifications/:id/acknowledge: writeAudit() added after
    db.update().returning() and after the 404 check.
    Event: "notification_acknowledged".

CHANGE 4 — FIX-01c: Broadcast actor field correction
  File: artifacts/api-server/src/routes/notifications.ts
  - POST /notifications/broadcast: Changed actor: user.userId to
    actor: user (the full JwtPayload object).
  - This ensures actorRole and actorId are correctly populated in
    the audit log for broadcast events.

CHANGE 5 — FIX-02: Locked pupils endpoint
  File: artifacts/api-server/src/routes/auth.ts
  - Added GET /auth/locked-pupils endpoint at line 91
  - Auth: authMiddleware + requireRole("coordinator", "head_teacher")
  - Queries usersTable: schoolId match, role "pupil",
    lockedUntil > new Date()
  - Returns: [{ id, firstName, lastName, className, yearGroup,
    lockedUntil, failedLoginAttempts }]
  - Returns empty array when no pupils are locked
  - Ordered by lockedUntil descending
  - Route declared BEFORE all parameterised /:id routes (placed at
    line 91, before /auth/pupil/start at line 117)
  - Added requireRole to import on line 10
  - FIX-02 unlock path: Using existing reset-pin endpoint
    (POST /api/schools/pupils/reset-pin/:pupilId) — no new endpoint

CHANGE 6 — FIX-02 UI: Locked pupils card
  File: artifacts/safeschool/src/pages/dashboard/CoordinatorDashboard.tsx
  - Added Lock, Unlock to lucide-react imports
  - Added useQuery for GET /api/auth/locked-pupils (uses BASE_URL prefix)
  - Added handleUnlockPupil function calling existing reset-pin endpoint
  - Added unlockingId and unlockSuccess state for per-pupil loading/
    confirmation
  - Amber warning card renders in Overview tab between stat cards grid
    and training completion card
  - Card only renders when lockedPupils.length > 0
  - Each pupil row shows: name, class/year group, lock type (admin reset
    vs timed), failed attempt count
  - "Reset lockout" button per pupil, shows spinner while in progress,
    shows "Reset sent" confirmation on success, refetches list

========================================================================

FILES MODIFIED
------------------------------------------------------------------------
1. artifacts/safeschool/src/pages/dashboard/PupilDashboard.tsx
2. artifacts/api-server/src/routes/diary.ts
3. artifacts/api-server/src/routes/notifications.ts
4. artifacts/api-server/src/routes/auth.ts
5. artifacts/safeschool/src/pages/dashboard/CoordinatorDashboard.tsx

========================================================================

ISSUES ENCOUNTERED
------------------------------------------------------------------------
1. Code review flagged that the unlock flow did not display the new PIN
   returned by the reset-pin endpoint. Fixed: handleUnlockPupil now
   reads the response JSON and stores { pupilId, newPin } in state.
   The UI renders the new PIN in a green badge next to the pupil row
   so the coordinator can communicate it to the pupil.

========================================================================

SERVER STARTUP CONFIRMATION
------------------------------------------------------------------------
API Server: Started cleanly on port 8080
  - "[cron] Pattern detection scan scheduled every 60 minutes"
  - "Server listening on port 8080"
  - "[db] Audit log immutability trigger applied"
  - "[seed] Demo seeding skipped (DEMO_MODE is not enabled)"
  - No errors

Frontend (Vite): Started cleanly
  - "VITE v7.3.1 ready in 302 ms"
  - No compilation errors
  - Hot module replacement confirmed working for changed files

========================================================================

VERIFICATION RESULTS
------------------------------------------------------------------------
1. Both servers start cleanly with no errors: PASS
2. GET /api/auth/locked-pupils returns 401 without auth: PASS
3. Frontend compiles without errors: PASS
4. e2e tests: PASS (status: "success")
   - Locked-pupils endpoint 401 check confirmed
   - Frontend renders without JavaScript/React errors
   - No console errors related to changes

========================================================================

CONSTRAINTS COMPLIANCE
------------------------------------------------------------------------
- FIX-00 implemented and verified first: YES
- Changes limited to listed files and functions: YES
- writeAudit() calls after successful DB operations: YES
- No new npm packages: YES
- No schema changes: YES
- Static routes before parameterised routes: YES
- UrgentHelpDialog untouched beyond type field: YES

========================================================================
END OF REPORT
========================================================================
