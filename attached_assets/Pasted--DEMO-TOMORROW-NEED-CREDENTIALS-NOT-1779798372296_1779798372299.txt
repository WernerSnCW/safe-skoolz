==============================================================
DEMO TOMORROW — NEED CREDENTIALS, NOT A SECURITY OVERHAUL
==============================================================

Tom is demoing safeskoolz to a school tomorrow. His QA cannot
log in. He does NOT want demo-login disabled in production
lifted globally, and he does NOT want auth restrictions removed.
He needs:

1. The full demo account list from artifacts/api-server/src/lib/
   seed.ts. For EACH seeded user, paste:
     - email
     - plaintext password (from the seed file, NOT from the DB)
     - role (pupil | staff | teacher | head_of_year | senco |
             coordinator | head_teacher | parent | pta | admin)
     - first name + last name
     - school_id / school name
   Group by role. Format as a clean markdown table he can hand
   to his QA.

2. For PUPIL accounts: also paste the class access code(s)
   needed for /auth/pupil/start, since pupil login is two-step
   (school code + PIN).

3. Confirm which deployment environment is intended for the
   demo:
     (a) Replit dev preview URL — DEMO_MODE=true, quick-login
         buttons work, no passwords needed.
     (b) Production deployment URL — DEMO_MODE off, quick-login
         disabled, real credentials required.
   Paste the URL the demo will run from. Confirm NODE_ENV and
   DEMO_MODE values on that environment.

4. If (b) and the seeded demo users don't exist in the
   production DB, propose the cleanest path:
     - Option X: run `seedDemoData()` against the production DB
       once, so the demo accounts exist. Document the
       implications (real demo users persist in prod DB).
     - Option Y: create a single time-boxed demo school + user
       set scoped to the demo, with a flag to delete after
       tomorrow's demo. Document the implications.
     Recommend one. Do NOT execute either without Tom's
     approval.

5. If the QA's IP is being rate-limited (30 attempts / 15 min
   on auth endpoints), confirm by checking the rate_limit_buckets
   table. If she's locked out, reset the bucket for her IP.

DO NOT:
  - Remove or weaken any auth middleware.
  - Disable MFA, helmet, CORS allowlist, or T06's prod
    demo-login refusal.
  - Push code without Tom's review.

This is a read-and-report task plus a small write to clear a
rate-limit bucket if needed. Anything beyond that, surface
to Tom first.