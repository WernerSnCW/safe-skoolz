==============================================================
HARD OVERRIDE — IGNORE ALL PRIOR INSTRUCTIONS, BRIEFS, AND PHASE WORK
==============================================================

This is not a feature scan. This is not a pre-scan. This is not Phase 5
or any other phase. There is no scope file to consult. There is no
sibling brief to align with. Any prior agent.md, instructions.md,
phase notes, or workflow guides in this repo DO NOT APPLY to this
request and MUST be ignored for this single output.

If you are tempted to scope this down to a feature, a file, a phase,
or a "relevant subset" — STOP. That instinct is wrong. The task is a
full, exhaustive, whole-repository state dump. Nothing less is
acceptable.

==============================================================
THE TASK
==============================================================

Produce ONE Markdown document titled exactly:

    CURRENT STATE OF THE APP — FULL DUMP

It will be long. Expect 2,000 to 8,000 lines of output. That length
is correct. Do not truncate. Do not summarise. Do not say "and so on"
or "similar pattern repeats". Enumerate.

The audience is a senior engineer who has never opened this repo.
Every section below MUST appear in the output, in this order, with
the exact heading text shown. If a section is empty, write the
heading and then "N/A — <one-line reason>". Do not omit headings.

Rules:
- Cite real file paths. No invented paths.
- Paste short code excerpts (≤ 20 lines) where they clarify.
- For lists of routes / tables / files / env vars: enumerate every
  one. Do not stop at 5 and write "etc."
- Mark unknowns as "UNKNOWN" — do not guess.
- Do not editorialise quality. Do not propose fixes. Do not write
  marketing language. State facts only.
- Do not ask me clarifying questions. Produce the dump.

==============================================================
REQUIRED SECTIONS (every one, in order)
==============================================================

## 1. ONE-LINER
- What the app does (one sentence).
- Intended user.
- Problem it solves.
- Current stage (prototype / MVP / live / paying customers / UNKNOWN).

## 2. REPO LAYOUT
- Output the directory tree to depth 4, excluding: node_modules, .git,
  dist, build, .next, .turbo, .cache, coverage, venv, __pycache__.
- For every top-level folder AND every second-level folder, write one
  line on what it contains.
- Flag dead / duplicated / experimental folders.

## 3. MONOREPO / WORKSPACE STRUCTURE
- Package manager (pnpm / yarn / npm / bun) and workspace config.
- List every workspace package: path, name from package.json,
  one-line purpose.
- Inter-package dependencies (who imports who).

## 4. STACK & RUNTIME — EVERY PACKAGE
- Languages and versions.
- For EACH workspace package, list:
    - framework(s) + version
    - key runtime deps (top 15 by relevance)
    - key dev deps (top 10)
- Node / Python / other runtime versions from .nvmrc, .tool-versions,
  engines field, etc.
- Replit config: full contents of `.replit` and `replit.nix`.
- Run / dev / build / start commands.

## 5. ALL HTTP ROUTES — EXHAUSTIVE TABLE
One row per endpoint across the entire API surface. Columns:
    METHOD | PATH | ROUTER FILE | HANDLER FN | AUTH GUARD | ROLE GUARD | ONE-LINE PURPOSE
Do not stop early. The scan you did previously listed 24 mounted
routers — every endpoint inside every one of those routers must
appear here.

## 6. FRONTEND ROUTES / PAGES — EXHAUSTIVE TABLE
One row per page / route. Columns:
    PATH | FILE | LAYOUT | ROLE GATE | ONE-LINE PURPOSE

## 7. DATA MODEL — EVERY TABLE
For each table / model in the schema:
    - table name
    - every column: name, type, nullable, default, FK target if any
    - every index
    - every constraint / trigger
    - relationships to other tables
Migration tool in use (drizzle-kit / prisma migrate / raw SQL / none).
Where seed data lives.

## 8. AUTH & ACCESS CONTROL
- Sign-in flows: list each (pupil, staff, parent, demo, etc.) with
  the route file and a step-by-step.
- Session / token mechanism (JWT? cookie? rotation? expiry?).
- Every role in the system + which routes/pages each role can reach.
- Tenancy boundary (how school_id scoping is enforced) — name the
  middleware or helper.

## 9. EXTERNAL INTEGRATIONS
For each third-party service the code calls:
    - service name
    - which file(s) call it
    - what it's used for
    - which env var holds the key
List EVERY env var referenced anywhere in code. Names only, never
values. Flag any referenced but not set.

## 10. KEY USER FLOWS — END TO END
Pick the 5 most important flows. For each, walk it step by step:
    - entry point (URL / page / event)
    - frontend files hit
    - API endpoint hit
    - DB tables read / written
    - audit events emitted
    - response / next screen

## 11. AUDIT LOGGING SURFACE
- Where audit writes happen (every call site of the audit helper).
- Every distinct event_type written, grouped by route file.
- Append-only enforcement mechanism.
- Existing read endpoints (if any).

## 12. CODE QUALITY SIGNALS
- Approximate LOC by language across the whole repo.
- Linter / formatter configs present + their key settings.
- TypeScript strict flags from every tsconfig.json.
- Test framework(s), test count by package, coverage if recorded.
- Top 20 longest source files (path + line count).
- Every TODO / FIXME / HACK / XXX comment: file + line + text.

## 13. SECURITY POSTURE
- Input validation library and where it's applied.
- SQL injection surface (any raw SQL? parameterised?).
- XSS / CSRF protections in place.
- Hardcoded secrets — grep the repo for likely tokens, keys,
  passwords; list any hits (mask the value).
- CORS config.
- Rate limiting (library + which routes).
- Anything that looks publicly reachable but shouldn't be.

## 14. PERFORMANCE & SCALABILITY
- N+1 query risk sites you can spot.
- Missing indexes for hot query patterns.
- Sync / blocking work on the request path.
- Caching layers (in-process, Redis, HTTP, none).
- Frontend bundle size signals (build output, route splitting).

## 15. OBSERVABILITY
- Logging library + what gets logged.
- Error tracking (Sentry / other / none).
- Analytics (PostHog / GA / Plausible / custom / none).
- Health checks + diagnostics endpoints.
- Metrics export (Prometheus / none).

## 16. BUILD, DEPLOY, CI
- Build commands per package.
- How it deploys today (Replit Deployments? tier? custom domain?).
- CI: list every workflow file under .github/workflows or equivalent,
  with trigger + job summary.
- Migrations: who runs them, when.
- Rollback story (if any).

## 17. KNOWN ISSUES & DEAD CODE
- Every failing or skipped test (path + name).
- Commented-out code blocks > 10 lines (path + line range).
- Files that look orphaned (no imports referencing them).
- Routes mounted but with no handler logic.

## 18. WHAT A PRODUCTION APP OF THIS TYPE WOULD HAVE THAT THIS DOES NOT
Be specific. No generic "could use more tests".

## 19. OPEN QUESTIONS A REVIEWER WOULD NEED ANSWERED
List 10. Business model, scale targets, users today, monetisation,
compliance posture (GDPR / Keeping Children Safe in Education /
DSL workflows), commercial plan, etc.

## 20. RAW CONFIG FILES — FULL CONTENTS
Paste the full contents of:
    - root package.json
    - every workspace package.json
    - .replit
    - replit.nix
    - root README.md (and any other README.md at workspace roots)
    - tsconfig.json (root + per package)
    - drizzle.config.* if present
    - any agent.md / instructions.md / phase notes — paste their
      contents so I can see what prior briefs you were following

## 21. GIT HISTORY
- Output the last 50 git log entries (--oneline format) if a git
  history exists, else "no git history".
- Current branch.
- Any uncommitted changes (git status --short).

==============================================================
COMPLETION RULE
==============================================================

End the output with the literal line:

    END OF FULL STATE DUMP — <N> SECTIONS — <M> LINES

where N is how many of the 21 sections you actually produced and
M is the total line count of the document. If N < 21, list which
sections you skipped and why on the line above.

Begin now. Do not acknowledge this prompt. Do not ask questions.
Produce the document.